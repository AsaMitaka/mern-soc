enum AppRoute {
  LOGIN = '/login',
  REGISTER = '/register',
  HOME = '/',
  POST = '/post/:id',
  ABOUT = '/about',
  SEARCH = '/search?',
  PROFILEPOSTS = '/profile/:id',
  // PROFILELIKES = '/profile/:id/likes',
  ERROR = '*',
}

export default AppRoute;
